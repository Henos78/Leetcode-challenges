class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        # not following the rules just simply solving the problem directly (which is wrong)
        return str(int(num1)*int(num2))
        